---
---
<header class="apple-header">
	<nav>
		<a href="/" class="logo"> MyBlog</a>
		<div class="links">
			<a href="/blog">文章</a>
			<a href="/about">关于</a>
		</div>
	</nav>
</header>

<style>
	.apple-header {
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 52px;
		background: rgba(251, 251, 253, 0.8);
		backdrop-filter: blur(12px); /* 核心：毛玻璃效果 */
		-webkit-backdrop-filter: blur(12px);
		border-bottom: 1px solid rgba(0, 0, 0, 0.05);
		z-index: 1000;
		display: flex;
		justify-content: center;
		align-items: center;
	}

	nav {
		width: 100%;
		max-width: 980px;
		display: flex;
		justify-content: space-between;
		padding: 0 20px;
		font-size: 14px;
		font-weight: 400;
	}

	a {
		color: var(--text-main);
		text-decoration: none;
		transition: opacity 0.2s ease;
	}

	a:hover {
		opacity: 0.7;
	}

	.logo {
		font-weight: 600;
		font-size: 16px;
	}

	.links a {
		margin-left: 24px;
		color: var(--text-muted);
	}
</style>